Check the current feature gap status between Android and web/desktop clients.

Steps:
1. Read `docs/specs/feature-gap-analysis.md` for the current gap analysis
2. Check the latest git log to see what's been implemented recently
3. Cross-reference against the web client features (check source at ~/git/stoat-web/ and ~/git/revolt-frontend/)
4. Report:
   - Features completed since last check
   - Next highest-priority gaps to address
   - Any new features discovered in web/desktop that aren't in the gap analysis
5. Update `docs/specs/feature-gap-analysis.md` if status has changed
