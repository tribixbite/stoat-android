Update the architecture spec documents in `docs/specs/` to reflect current implementation state.

Steps:
1. Read the current spec file(s) in `docs/specs/`
2. Read the relevant source code to understand current state
3. Update the spec to reflect:
   - New features added
   - TODOs completed (mark with [x])
   - New TODOs discovered
   - Architecture changes
   - API behavior changes
4. Update `docs/specs/README.md` table of contents if needed
5. Keep specs concise and factual - no aspirational content, only what exists or is planned
